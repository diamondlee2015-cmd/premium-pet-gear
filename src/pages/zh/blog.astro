---
// src/pages/zh/blog.astro

// 1. 引入“读取原料”的工具函数
import { fetchPosts } from '~/utils/blog'; 

// 2. 引入布局和排版组件
import BlogList from '~/components/blog/List.astro'; 
import NesthingLayout from '~/layouts/NesthingLayout.astro';

// 3. 先把所有的原始文章抓出来，命名为 allPosts
const allPosts = await fetchPosts(); 

// 4. 过滤：只保留以 'zh/' 开头的中文文章，命名为 posts 喂给组件
const posts = allPosts.filter(post => post.slug.startsWith('zh/'));
---

<NesthingLayout metadata={{ title: '技术博客' }}>
  <BlogList posts={posts} />
</NesthingLayout>